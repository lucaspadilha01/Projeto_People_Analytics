One-line: icon-only control for top bars and card headers; always pass `label`.

```jsx
<IconButton label="Notifications" badge><Icon name="bell" /></IconButton>
```

Tones: `quiet` (transparent, muted glyph — the top-bar default), `tinted` (violet-50 tile), `accent`.
