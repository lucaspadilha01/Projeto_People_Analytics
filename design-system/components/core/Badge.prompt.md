One-line: 11px status pill; `warn` is the "Pending" state on vendor payments, `accent` the task category tag.

```jsx
<Badge tone="warn">Pending</Badge>
<Badge tone="accent">Design</Badge>
<Badge tone="ok" dot>On-track</Badge>
```
