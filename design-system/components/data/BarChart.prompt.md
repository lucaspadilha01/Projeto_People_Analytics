One-line: all NexaFlow bar charts; series colours come from `--series-1..4` (violet on violet — never a rainbow palette).

```jsx
<BarChart yTicks={['100','75','50','25','0']} stacked
  data={[{label:'Jan',values:[30,12,8]},{label:'Feb',values:[38,14,8]}]} />
```
