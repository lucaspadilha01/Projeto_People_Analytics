One-line: the product's left rail; `NEXAFLOW_NAV` is the canonical order (Dashboard → Projects → Materials → Timeline → Budget → Approvals → Team → Settings → Logout).

```jsx
<SidebarNav active="budget" onSelect={setScreen} brand={<Logo />} />
```
