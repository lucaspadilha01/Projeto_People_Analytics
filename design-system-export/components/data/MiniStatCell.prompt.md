One-line: tiny tinted counter cell; use tone to carry the meaning (`ok` completed, `warn` delayed).

```jsx
<MiniStatCell label="Completed" value="5" tone="ok" />
<MiniStatCell label="Delayed" value="1" tone="warn" />
```
