One-line: completion ring — the most recognisable NexaFlow element; one per card, centred.

```jsx
<DonutGauge value={65} sublabel="Complete" />
<DonutGauge value={73} sublabel="Allocated" size={116} />
```
